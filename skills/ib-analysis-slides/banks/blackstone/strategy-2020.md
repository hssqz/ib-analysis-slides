# Investment Strategy 2020

Use a720×540 canvas with left/right edges about23/696. A small top identity/date row precedes a sans bold topic at18pt and a thin black line; a Georgia claim around20pt appears below; then a thick black rule. The topic names the subject, while the claim interprets the evidence. Do not substitute the2025 fund's section-label/serif-title shell. Examples use Arial in place of unbundled Calibri.

Colors include cyan#0097C3, teal#006778, aqua#62CEC9, lime#C8DD03, magenta#B8005C and gray#636363. Their role follows the series/category, not positive/negative sentiment by default.

## Complementary macro evidence

[B6](examples/index.html#B6) combines signed balance-sheet changes with their net total, a separate policy-rate series, and a share-of-banks comparison. Independent units and observation windows are explicit. Positive and negative components stack separately about zero; the net line is their algebraic sum. Never draw the net line along the top of the positive stack. Forecast shading starts at the actual forecast boundary; each period stays equally spaced only when intervals are equal.

A net line may have values below zero even where positive contributions exist. Keep all components and the zero line readable. If a signed component is too small to read at the common scale, add its signed numerical label or a companion data row; do not inflate the bar. Historical-only input does not require forecast shading. Missing forecasts remain explicitly missing rather than receiving inferred values. A selected panel's individual source date is not automatically the whole page's date. Source S29 uses different reporting windows across panels; the fictional example retains separate labels rather than copying historical dates.

For pros/cons(S13), four charts must provide four distinct facts; stance colors do not replace series legends. Dual axes need both units and labels. Source S13 includes an embedded image of one two-series chart, and its complete numerical series is not recovered. Editable vector preservation does not make that image a data-driven chart.

For survey+observation(S41), leftward bars encode answer direction, not negative people. Keep respondent groups, certainty subgroups, omitted response options and uncertainty distinct. A nearby temperature series is independent evidence, not a regression proving the survey's cause.

## Sensitivity

[B7](examples/index.html#B7) is a native HTML matrix with explicit, fictional formula: value=EPS×40%/(Treasury yield+5%−2%). Rows are EPS, columns yields; each displayed value is calculated from those inputs. The row/column headings are native headers. Use a consistent numerical color scale; do not remap independently per row.

This intentionally simpler perpetual model is **not** the source S19 dividend-discount model, which specifies a transition toward4% nominal growth,34% payout and3.7% equity risk premium. The private grid recovers all208 displayed cells and16 EPS/13 yield labels from PPTX, while the exact workbook formula remains unavailable. Do not substitute the fictional formula when reconstructing the source. If new input lacks a formula, a labeled assumptions table is an honest alternative to invented model outputs.

## Change conditions

Test additional actual/forecast periods, all-negative components, uneven dates, a missing series, signed survey percentages with omitted categories and longer sensitivity labels. Exact-time-series recovery, model inference and statistical fitting remain outside the tested examples. Register fresh source/evidence and rerender for each change.

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
